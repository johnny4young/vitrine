import VitrineCLICore

// The executable's only statement. The commands, the PRO gate, and the accessory
// NSApplication host live in VitrineCLICore's `CLICommandLine`, which the tests link too.
CLICommandLine.run()
